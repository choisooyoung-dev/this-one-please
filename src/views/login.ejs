<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Login Page</title>
        <link
            href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
            rel="stylesheet"
        />
        <link rel="stylesheet" href="<%= path %>stylesheets/style.css" />
        <style>
            .bg-blue-button {
                background-color: #60a5fa; /* Tailwind blue-400 */
            }
            .user-logo {
                margin-right: 40px;
            }
        </style>
    </head>
    <body class="flex items-center justify-center h-screen">
        <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full">
            <div class="flex justify-center mb-6">
                <div class="user-logo"><%-include('logo')%></div>
            </div>
            <form class="space-y-6">
                <div>
                    <label for="email" class="text-sm font-medium text-gray-700"
                        >아이디(Email)</label
                    >
                    <input
                        type="email"
                        id="email"
                        name="email"
                        required
                        class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
                        placeholder="이메일을 입력하세요"
                    />
                </div>
                <div>
                    <label
                        for="password"
                        class="text-sm font-medium text-gray-700"
                        >비밀번호</label
                    >
                    <input
                        type="password"
                        id="password"
                        name="password"
                        required
                        class="appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
                        placeholder="비밀번호를 입력하세요"
                    />
                </div>
                <div>
                    <button
                        type="submit"
                        class="w-full bg-blue-button text-white py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
                    >
                        로그인
                    </button>
                </div>
            </form>
            <div class="mt-6 text-center">
                <p class="text-sm font-medium text-gray-600">
                    계정이 없으신가요?
                </p>
                <div class="space-x-2">
                    <a href="/signup" class="text-blue-600 hover:text-blue-500"
                        >회원가입</a
                    >
                </div>
            </div>
        </div>
        <script src="<%= path %>javascripts/login.js"></script>
    </body>
</html>
