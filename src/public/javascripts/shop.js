document.addEventListener("DOMContentLoaded", function () {
  let storeId = document.getElementById("storeId").dataset.storeId;
  //같은 방식으로 store-name 스토어 이름만


    // menucontainer getbyid
  
  // fetch("/api/menu") 
  //{

  // var newDiv = document.createElement('div');
  // newDiv.classList.add('menu-card p-4');

  //response.data.forEach((e) => {
    //이미지 생성
    // newDiv.appendChild(이미지);

    //div 생성 name
    //name.inner.text = e.name
    // newDiv.appendChild();

    //div 생성 가격
    // newDiv.appendChild();

    // 버튼생성
    // newDiv.appendChild(버튼);
    //});
  //}
});